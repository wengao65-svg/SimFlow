"""Canonical SimFlow runtime core package.

This package is the stable import surface for workflow-layer state,
artifact, checkpoint, lineage, workflow, gate, and migration behavior.
The legacy ``runtime.lib`` modules remain as compatibility shims while
callers migrate to this package.
"""

