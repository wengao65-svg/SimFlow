{
  "name": "dry_run_default",
  "description": "Real local, remote, and scheduler execution is planned and validated before submission.",
  "scope": "real_execution",
  "rules": [
    {
      "condition": "real_execution_planned",
      "action": "build_immutable_run_plan"
    },
    {
      "condition": "run_plan_validation_failed",
      "action": "block_submission"
    }
  ]
}
